<Window x:Class="ManiaLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mania Launcher — by Mania AI"
        Height="650" Width="1000"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="16,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="MANIA" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text=" LAUNCHER" FontSize="22" FontWeight="Light" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
                <TextBlock Text="от maniacalkid · Mania AI" 
                           HorizontalAlignment="Right" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Profiles -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="ПРОФИЛИ" FontWeight="Bold" Margin="0,0,0,8" Foreground="{StaticResource TextSecondaryBrush}"/>
                    
                    <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                        <Button Content="➕ Новый профиль" Click="NewProfile_Click" Margin="0,0,0,6"/>
                        <Button Content="▶ Играть" Click="Play_Click" FontWeight="Bold"/>
                    </StackPanel>

                    <ListBox x:Name="ProfilesList" 
                             DisplayMemberPath="Name"
                             SelectionChanged="ProfilesList_SelectionChanged"/>
                </DockPanel>
            </Border>

            <!-- Right Panel: Tabs -->
            <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12">
                <TabControl Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="12,6"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border x:Name="Border" Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter ContentSource="Header"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>

                    <!-- Versions Tab -->
                    <TabItem Header="Версии">
                        <DockPanel Margin="0,12,0,0">
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                <Button Content="🔄 Обновить" Click="RefreshVersions_Click" Margin="0,0,8,0"/>
                                <Button Content="⬇ Установить выбранную" Click="InstallVersion_Click"/>
                            </StackPanel>
                            <ListBox x:Name="VersionsList" DisplayMemberPath="Id"/>
                        </DockPanel>
                    </TabItem>

                    <!-- Accounts Tab -->
                    <TabItem Header="Аккаунты">
                        <DockPanel Margin="0,12,0,0">
                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBox x:Name="OfflineUsernameBox" Width="180" Margin="0,0,8,0" 
                                         ToolTip="Ник для оффлайн-аккаунта"/>
                                <Button Content="Добавить оффлайн" Click="AddOffline_Click" Margin="0,0,8,0"/>
                                <Button Content="Добавить Microsoft" Click="AddMicrosoft_Click"/>
                            </StackPanel>
                            <ListBox x:Name="AccountsList">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Username}" FontWeight="SemiBold"/>
                                            <TextBlock Text="  (" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text="{Binding Type}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text=")" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </TabItem>

                    <!-- Logs Tab -->
                    <TabItem Header="Логи">
                        <TextBox x:Name="LogBox" 
                                 IsReadOnly="True" 
                                 TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 Background="{StaticResource BackgroundBrush}"
                                 Foreground="{StaticResource TextBrush}"
                                 BorderThickness="0"
                                 Margin="0,12,0,0"/>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Text="Готов" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
        </Border>
    </Grid>
</Window>
