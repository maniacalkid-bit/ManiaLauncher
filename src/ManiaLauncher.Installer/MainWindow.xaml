<Window x:Class="ManiaLauncher.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Установщик Mania Launcher"
        Height="420" Width="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource Bg}"
        Foreground="{StaticResource Text}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="MANIA LAUNCHER" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Установщик · от Mania AI (maniacalkid)" Foreground="{StaticResource Secondary}" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Папка установки:" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" IsReadOnly="True" Padding="8" Background="{StaticResource Surface}" BorderBrush="#333"/>
                <Button Grid.Column="1" Content="Обзор..." Margin="8,0,0,0" Padding="12,6" Click="Browse_Click"
                        Background="{StaticResource Accent}" Foreground="White" BorderThickness="0"/>
            </Grid>

            <CheckBox x:Name="DesktopShortcut" Content="Создать ярлык на рабочем столе" IsChecked="True" Margin="0,16,0,0" Foreground="{StaticResource Text}"/>
            <CheckBox x:Name="StartMenuShortcut" Content="Создать ярлык в меню Пуск" IsChecked="True" Margin="0,8,0,0" Foreground="{StaticResource Text}"/>
        </StackPanel>

        <ProgressBar x:Name="Progress" Grid.Row="2" Height="8" Margin="0,16,0,8" Background="{StaticResource Surface}" Foreground="#888"/>

        <Grid Grid.Row="3">
            <TextBlock x:Name="StatusText" Text="Готов к установке" VerticalAlignment="Center" Foreground="{StaticResource Secondary}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Width="90" Margin="0,0,8,0" Click="Cancel_Click"
                        Background="{StaticResource Accent}" Foreground="White" BorderThickness="0" Padding="0,8"/>
                <Button x:Name="InstallBtn" Content="Установить" Width="110" Click="Install_Click"
                        Background="#444" Foreground="White" BorderThickness="0" Padding="0,8" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
